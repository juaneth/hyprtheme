export function installTheme(theme: object) {
    // TODO: Install theme

    // Backup current theme

    // Git clone repo

    // Use theme config to ensure components installed

    // Use config to copy files to config directories for each component

    // Return success or failure
}

